# Core Django framework
Django>=5.0,<6.0

# Production server
gunicorn

# Static file handling (needed for Render)
whitenoise

# HTTP requests (used in odds API)
requests

# CORS support for frontend -> backend requests
django-cors-headers

# PostgreSQL support (for when you move off SQLite)
psycopg2-binary
dj-database-url